services:
  - type: worker
    name: telegram-ytdl-bot
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    # optionally pin region: 
    # region: iad
    # Ensure the repo is the current repo; Render will build from your repo.
    # You can set env vars in Render dashboard. See README for required vars.
    autoDeploy: true
