## Required environment variables
- `TELEGRAM_BOT_TOKEN` — your bot token (from BotFather)
- (optional) `TELEGRAM_MAX_BYTES` — max file size to attempt upload in bytes (default ~1.9GB)
- (optional) `CHAT_WHITELIST` — comma-separated chat ids to restrict bot

